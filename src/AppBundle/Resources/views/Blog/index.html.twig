{% extends 'AppBundle::layout.html.twig' %}

{% block content %}
    <ul>
        {% for blog in blogs %}
            <li><a href="{{ path('blog', {id: blog.id }) }}" title="">{{ blog.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
