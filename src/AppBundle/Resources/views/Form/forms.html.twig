{% block collection_widget -%}
    {% if prototype is defined %}
        {%- set attr = attr|merge({'data-prototype': form_row(prototype) }) -%}
        {%- set attr = attr|merge({'data-count': form.children|length}) -%}
    {% endif %}
    {{- block('form_widget') -}}
{%- endblock collection_widget %}
