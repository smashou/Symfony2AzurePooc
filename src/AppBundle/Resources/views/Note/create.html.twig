{% extends 'AppBundle::layout.html.twig' %}

{% block content %}
    <h1>{{ "Add a Note"|trans }}</h1>
    <div>
        {{ form_start (form) }}
            {{ form_widget(form) }}
            <button type="submit" name="submit" id="submit" class="btn btn-success" >Save</button>
        {{ form_end(form) }}
    </div>

{% endblock %}
