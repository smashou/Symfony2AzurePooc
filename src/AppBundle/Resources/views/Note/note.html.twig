{% extends 'AppBundle::layout.html.twig' %}

{% block content %}
    <div class="page-header">
        <h1>{{ note.title }}</h1>
    </div>
    <div>
        <p>
            {{ note.content }}
        </p>
    </div>

{% endblock %}
