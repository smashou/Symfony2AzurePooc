{% extends 'AppBundle::layout.html.twig' %}

{% block content %}
    <div class="page-header">
        <h1>{{ post.title }}</h1>
    </div>
    <div>
        <p>
            {{ post.content }}
        </p>
    </div>

{% endblock %}
